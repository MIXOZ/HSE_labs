\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[english,russian]{babel}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amsmath,amsfonts,amssymb,amsthm,mathtools} 


\usepackage{wasysym}

\usepackage{caption}
\usepackage{tikz}
\usepackage{subcaption}
\usepackage{imakeidx}
\usepackage[russian]{cleveref}
\usepackage[a4paper,left=15mm,right=15mm,top=30mm,bottom=20mm]{geometry}
\parindent=0mm
\parskip=3mm

\makeindex
\pagestyle{empty}

\title{ДЗ 2, Алгоритмы}
\author{Михаил Лопатин}
\date{15.09.2019}

\begin{document}

\maketitle

\begin{enumerate}

\section{Основная часть}

\item % Задание 1

a) Т.к. i повторяется $\log_{2}n$, а j от 0 до i  $\rightarrow$ всего j будет вызываться 1+2+4+...+n всего $\log_{2}n+1$ слагаемых тогда посчитаем сумму геометрической прогрессии $\frac{1(2^{\log_{2}n+1}-1)}{2-1}=2n$ $\rightarrow \Theta (n)$

b)i вызывается $\frac{n}{3}$ j от 0 до $\log i$ получим $\log3\cdot6\cdot9\cdot...\cdot n$ выносим 3 $\log3^{\frac{n}{3}}+\log\frac{n}{3}!=n+n\log n=\Theta(n\log n)$

c)i от 1 до n, j от $\frac{n}{i}$ до i, наибольшее количество итераций j=n будет при i=n-1 $\rightarrow$ асимптотика = $\Theta(n\cdot n)=\Theta(n^{2})$

d)i изменяется от n до 1, x от n до i $\rightarrow \Theta (n)$

\item

a)

const int MOD = 1e9; // 0 <= a[i], b[i] < MOD 
const long long A =7*1e18;
long long scalarProductMod(int n, long long *a, long long *b) $\{$ 

long long sum = 0; 

for (int i = 0; i < n; i++)$\{$ if (sum>A) sum = (sum + a[i] * b[i]) $\%$ MOD; $\}$

return sum; 

$\}$

Операция деление с остатком выполняется долго, поэтому ее следует выполнять только когда long long может переполниться.

b)

 double Exp(double x, int dep=1, double F=1) { 
 
 int s = 1; 

 int k=1;

 k=1;
 for (dep=1;dep<20;++dep;)$\{$
 
  k=x*k;
 
 s+=k/F;
 

 
 F=F*(dep+1);$\}$
 
 return ;
 
 } 
 
 Функция возведения в степень работает медленно поэтому ,будем передовать предидущую степень и домножать на X, так же перепишем рекурсию в виде цикла чтобы каждый раз не вызывать функцию.
 
 c)Каждый раз долго вызывается to$\_$string и операция сложения строк.


\item

Пойдем в цикле от 1 до n и от j-1 до n тогда если суума больше чем s выходим из второго цикла увеличиваем i на 1 и запускаем цикл от j-1 до n, тогда сумма на отрезке от i до j-1 < s  и следовательно сумма на отрезке от i+1 до j-1 тоже будет < s, если же сумма = s выводим i и j и завершаем программу.

\end{enumerate}






























\end{document}